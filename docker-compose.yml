services:
  jekyll:
    image: jekyll/jekyll:latest
    command: >
      sh -c "gem install webrick && jekyll serve --watch --force_polling"
    ports:
      - "4010:4000"
    volumes:
      - .:/srv/jekyll